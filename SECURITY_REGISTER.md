# Security register — Zendesk_App

Low-severity and tooling/docs findings from security reviews. One row per
finding; open rows are swept into one batch cleanup PR per security train
cycle. Filing policy: docs/SECURITY_REVIEW_TAXONOMY.md in
shawnbray205/tsanet-ops.

| date | sev | subject | finding | disposition | review-by |
|---|---|---|---|---|---|
| 2026-08-18 | MED | audit-tooling | The committed audit record sat at 4cbca06 for 20 days while 61 commits (three new member-reaching features, releases v1.0.61-v1.0.69) shipped, every release publishing flagged NOT covered; a 2026-08-01 review session at 2cfd42e never committed its record advance. Coverage gap resolved by the 2026-08-18 record advance to 2657868; the open residual is process: a review's record commit is not part of its definition of done, and no probe notices a review session newer than the committed reviewed_at. | open | |
| 2026-08-18 | MED | docs | SECURITY_CONSIDERATIONS.md does not record the auto-accept trust delta: with tsanet_auto_accept ON, a forged inbound delivery (callbackAuth Basic compromise; HMAC unverifiable per the accepted #90 limitation) yields an automatic org-level case acceptance with no human action. Controls exist and were verified (default OFF; the guard compares a deploy-baked constant, so a forged event cannot enable it; fail-safe polarity; eventType guard; failure note; drift surfaced on the deploy screen; QUICK_START documents effect-at-next-deploy) but the residual is stated nowhere a security reviewer or an enabling member would read, so #90's accepted limitation has silently absorbed a raised consequence without re-affirmation. MED per taxonomy category 6: a compensating-control story that does not name the residual. Fix: one paragraph in SECURITY_CONSIDERATIONS.md and the auto-accept section naming the boundary (callbackAuth Basic), what auto-accept commits without a human, that disabling = save setting + redeploy, and a rotation-cadence recommendation for members who enable it. | open | |
| 2026-08-18 | LOW | docs | Three data-inventory lines lag the window's features. (a) PII_Retention_and_Data_Handling.md's "Description (first comment)" row predates #223 and does not name the partner process-form field values the v1.0.68+ bundle appends to the first comment (redaction guidance unaffected, same artifact and path). (b) The same map does not name the agent-typed Accept "additional instructions" (#216) as an artifact stored on TSANet's side. (c) SECURITY_CONSIDERATIONS.md's outbound table's case-data row does not name internal Zendesk ticket ids as an identifier class disclosed to the partner (#213/#215). | open | |
| 2026-08-18 | LOW | audit-tooling | No mechanical check inventories client-side network sinks (fetch / client.request / cors:true), so a sixth outbound destination would be caught only by manual review; manifest domainWhitelist constrains the ZAF proxy path, not direct browser fetches (receipt: the documented cors:true api.github.com call coexists with a two-host whitelist). The outbound-destinations table is prose, not enforcement. Fix candidate: a security-audit.py check that greps the three HTML/JS surfaces for network sinks and compares hosts against the documented table. | open | |
